<% if flash.any? %>
    <% user_facing_flashes.each do |key, value| %>
        <div class="alert alert-dismissible alert-<%= flash_class(key) %>">
          <button class="colse" data-dismiss="alert">  X</button>
          <%= vlue %>
        </div>
    <% end %>
<% end %>
